#!/bin/sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
PROJECT_DIR=$(dirname "$SCRIPT_DIR")

cd "$PROJECT_DIR"
mkdir -p data/logs

if [ -f ".env" ]; then
  set -a
  . "./.env"
  set +a
fi

exec "$PROJECT_DIR/scripts/python" -m job_agent.app
